package csc1035.project3;
import org.hibernate.Session;

public class Main {
    /**
     *This is a main class used for testing features which have been implemented. This can be changed/deleted later on.
     */
    public static void main(String[] args) {

        //This code tests a product insertion into the database. The code can be deleted when required as it was used for testing.
        /**
        Products p = new Products(0,"NewProduct2","Products",false,4.50,10,10.0);
        Session session = HibernateUtil.getSessionFactory().openSession();
        session.beginTransaction();
        session.save(p);
        session.getTransaction().commit();
        session.close();
*/
}

}
